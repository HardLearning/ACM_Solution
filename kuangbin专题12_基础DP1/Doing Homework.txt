题意：N个任务，有截止时间和花费时间。超时要罚款，罚款数为超时天数之和。

分析：题目还要求字典序输出，不能用贪心的思路了。
N<=15，考虑状态压缩DP。
一个数X的二进制形式是01字符串，1表示某项任务已经完成，0表示未完成。
比如说111可以从110，101，011转移过来，取花费代价最小的。
最后的状态即为DP[(1<<N)-1],用一个pre数组记录从之前的哪个状态转移。
