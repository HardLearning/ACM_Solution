题意：给定一个N阶的01方阵，问如何调换矩阵的行或列，使得矩阵的对角线元素都为1

分析：若矩阵对角线元素全为1，则矩阵满秩
先求二分图最大匹配，若匹配数<n则无论如何调换都不能满足题意
匹配是把矩阵的行和列分别作为二分图的左右点集。

注意，学过线性代数应该知道 若有解，只交换行或只交换列都可以到达效果。
那么只要让match[i] == i 即可 
match[i] 记录的是第i列匹配的行