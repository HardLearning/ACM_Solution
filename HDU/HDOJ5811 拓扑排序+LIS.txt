题意：N个人，有等级排行。
现在分成两堆人，要求两队的人等级排序都是严格递增或递减的。
T1有M人，T2有N-M人。。
假如是有严格的等级序列，问从T2最多能抽取多少人到T1，使得T1还是严格的等级排序。
题目会给出T1的M个人的编号。

分析：拓扑排序+LIS
既然等级是严格递增或递减的，那就拓扑排序判断是否成立。。
然后枚举T2的每个人能否放入T1内，可以的话记录放置的位置。O(n^2)
因为T1的排序是严格递增的，所以插入的位置可以唯一确定。
最后对T2每个人可以插入的位置求最长递增子序列 O(nlogn)。